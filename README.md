# Urban Street Ventures Email Signature

HTML email signature for Robert Compean, to be pasted into Gmail's signature setting.

## Open items

The following placeholders in `urbansv-signature.html` still need real values:

**Image URLs** (host on urbansv.com, e.g. via WordPress Media Library):
- `LOGO_URL`
- `FACEBOOK_ICON_URL`
- `LINKEDIN_ICON_URL`
- `INSTAGRAM_ICON_URL`
- `TIKTOK_ICON_URL`
- `YOUTUBE_ICON_URL`

Profile links and the Cash Offer Calculator link are filled in.

## Remaining work

Once the above URLs are provided, fill in the placeholders and test the rendered signature across email clients (Gmail, Outlook, mobile mail apps) before setting it as the Gmail default.
